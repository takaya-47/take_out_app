<%# deviseに紐付かないモデルのエラーメッセージ %>
<% if @menu.errors.any? %>
<div class="error-explanation">
  <ul>
    <% @menu.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>