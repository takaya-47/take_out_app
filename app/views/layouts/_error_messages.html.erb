<%# deviseに紐付かないモデルのエラーメッセージ %>
<% if model.errors.any? %>
<div class="error-explanation">
  <ul>
    <% model.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>